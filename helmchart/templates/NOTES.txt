🚀 Backend deployed successfully!

Backend:
  kubectl port-forward svc/{{ include "backend.fullname" . }} 8000:8000

PostgreSQL:
  Host: backend-postgresql
  DB: {{ .Values.postgresql.auth.database }}

PgAdmin:
  kubectl port-forward svc/{{ include "backend.fullname" . }}-pgadmin 8080:8080
